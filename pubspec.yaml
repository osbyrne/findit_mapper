name: findit_mapper
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1
webview_flutter: any
environment:
  sdk: ">=2.19.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  gsheets: ^0.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
